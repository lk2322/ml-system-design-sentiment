# Модель бизнес-процесса «КАК ДОЛЖНО БЫТЬ» (To-Be)

## Описание целевого процесса

После внедрения ИИ-системы **Sentinel AI** анализ тональности и модерация автоматизированы: ML-модели реальном времени классифицируют каждый комментарий, принимают предварительные решения и маршрутизируют пограничные случаи на ручную проверку. Модераторы концентрируются на сложных случаях, а не на массовом потоке.

## BPMN-диаграмма (To-Be)

```mermaid
flowchart TD
    Start([Пользователь оставляет комментарий]) --> Ingest[Сбор комментария через API шлюз]
    Ingest --> Enrich[Обогащение: метаданные пользователя, контекст поста]
    Enrich --> NLP[ИИ-анализ: тональность + токсичность + тематика]
    NLP --> Classify{Результат классификации ИИ}
    
    Classify -->|Уверенный негатив / токсичность > порога| AutoAction[Автоматическое действие]
    Classify -->|Уверенный позитив / нейтрал| AutoApprove[Автоматическое одобрение]
    Classify -->|Низкая уверенность| Escalate[Маршрутизация модератору]
    
    AutoAction --> ActionType{Тип действия}
    ActionType -->|Удаление| Delete[Комментарий удалён]
    ActionType -->|Скрытие| Hide[Комментарий скрыт]
    ActionType -->|Предупреждение| Warn[Автор получает предупреждение]
    
    Escalate --> ModReview[Модератор просматривает с подсказками ИИ]
    ModReview --> ModDecision{Решение модератора}
    ModDecision -->|Подтвердить ИИ| AutoAction
    ModDecision -->|Отклонить ИИ| AutoApprove
    ModDecision -->|Удалить| Delete
    ModDecision -->|Предупредить| Warn
    
    AutoApprove --> Analytics[Данные → аналитический дашборд]
    Delete --> Analytics
    Hide --> Analytics
    Warn --> Analytics
    
    Analytics --> Trends[Выявление трендов тональности]
    Trends --> Alert{Всплеск негатива?}
    Alert -->|Да| Notify[Уведомление PR / менеджерам]
    Alert -->|Нет| Store[Хранение в Data Lake]
    
    Notify --> Store
    Store --> End([Процесс завершён])
    
    Escalate --> FeedbackLoop[Данные → дообучение модели]
    ModDecision --> FeedbackLoop
    FeedbackLoop --> NLP

    style NLP fill:#ccffcc,stroke:#006600
    style AutoAction fill:#ffcccc,stroke:#cc0000
    style AutoApprove fill:#ccffcc,stroke:#006600
    style FeedbackLoop fill:#ccccff,stroke:#0000cc
    style Analytics fill:#ffffcc,stroke:#cc9900
```

## Что меняет ИИ-система в бизнес-процессе

| Аспект | As-Is (ручной) | To-Be (с ИИ) |
|---|---|---|
| **Охват** | 3–5% комментариев | 100% комментариев |
| **Скорость реакции** | 4–24 часа | < 1 секунда для 85%+ случаев |
| **Объективность** | Субъективная оценка модератора | Единая модель с настраиваемыми порогами |
| **Стоимость** | Линейный рост ФОТ | Фиксированные расходы на инфраструктуру + модель |
| **Риск штрафов РКН** | Высокий | Минимальный (полное покрытие) |
| **Аналитика** | Отсутствует | Реалтайм дашборды, тренды, прогнозы |

## Участники процесса (To-Be)

| Участник | Роль |
|---|---|
| Пользователь | Оставляет комментарии |
| ИИ-система Sentinel AI | Автоматическая классификация, принятие решений, аналитика |
| Модератор | Обработка пограничных случаев (escalation), валидация решений ИИ |
| PR-менеджер / контент-менеджер | Реагирование на всплески негатива по уведомлениям |
| Data Engineer | Поддержание пайплайнов данных, переобучение модели |

## Количественные цели (To-Be)

| Показатель | Целевое значение |
|---|---|
| Охват комментариев | 100% |
| Доля автоматических решений | ≥ 85% |
| Среднее время реакции на токсичный контент | < 1 секунда |
| Доля эскалаций модераторам | ≤ 15% |
| Точность классификации (F1) | ≥ 0.92 |
| Снижение риска штрафов РКН | До минимального |